/*
  ChatInput.jsx
  -------------
  Text input + send button at the bottom of the chat panel.
  Dispatches user messages to the chat Redux slice.
*/

const ChatInput = () => {
  return <div className="p-2">ChatInput</div>;
};

export default ChatInput;
