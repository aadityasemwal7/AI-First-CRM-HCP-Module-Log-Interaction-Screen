/*
  ChatWindow.jsx
  --------------
  Main chat container that renders the message list
  and the input bar. Connects to the LangGraph-powered
  /chat endpoint via Redux.
*/

const ChatWindow = () => {
  return <div className="p-4">ChatWindow</div>;
};

export default ChatWindow;
